// The old standalone debug flow used pre-master scene ids and a fixed 22:00 anchor.
// Keep the debug entry pointed at the canonical player so it cannot drift from story data.
export { NarrativePlayer as NarrativeDebugPlayer } from '../ui/NarrativePlayer';
